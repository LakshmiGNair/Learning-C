#include <stdio.h>
int main()
{ printf("hello");
return 0;
}


// This is a simple C program that prints "hello" to the console. It includes the standard input/output library and defines the main function, which is the entry point of the program. The program returns 0 to indicate successful execution.

// To compile and run this program, you can use a C compiler such as `gcc`. For example, you can save the code in a file named `first.c`, then open a terminal and run the following commands:
// In this program the first line is #include <stdio.h> which is a preprocessor directive that tells the compiler to include the standard input/output library. This library provides functions for performing input and output operations, such as printing to the console.
// The main function is defined as int main(), which is the entry point of the program. The code inside the main function is executed when the program runs. In this case, it calls the printf function to print "hello" to the console. Finally, the program returns 0 to indicate that it finished successfully.